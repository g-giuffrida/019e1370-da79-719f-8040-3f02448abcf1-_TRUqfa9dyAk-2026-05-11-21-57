export default function PipelineVisual() {
  return (
    <img
      src="/project-failure.png"
      alt="AI-assisted failure detection pipeline: data ingestion, AI processing, classification, root cause analysis, insights"
      className="w-full h-auto"
      style={{ mixBlendMode: "multiply" }}
      loading="lazy"
    />
  );
}
