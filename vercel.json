{
  "buildCommand": "cd webapp && npm ci --prefer-offline && npm run build",
  "outputDirectory": "webapp/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
